#include <bits/stdc++.h>

using namespace std;

void solve(){

}

int main(){
	string a, b;
    int c, d;
    cin >> a >> b;
    c = stoi(a);
    d = stoi(b);
    string ans = a + b;
    cout << ans << " " << c + d;
	return 0;
}